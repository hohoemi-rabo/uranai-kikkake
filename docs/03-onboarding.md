# 03. オンボーディング

> 関連: REQUIREMENTS.md §4.1.1, §5.5

## 概要

初回起動時のオンボーディング(3〜4 スライド)を実装。ログイン画面の前に表示し、設定画面から再表示できるようにする。

## 前提

- [01. プロジェクト基盤](./01-project-foundation.md)

## TODO

- [ ] `app/(auth)/onboarding.tsx` 作成(3〜4 スライドのスワイプ)
- [ ] スライド内容: (1)できること(魅力発見・占い)/ (2)エンタメであること明示 / (3)スマホ推奨 / (4)はじめましょう
- [ ] AsyncStorage に `onboarding_completed` フラグを保存
- [ ] ルート `_layout.tsx` で初回判定 → 未完了なら `/onboarding` へ Redirect
- [ ] 「次へ」「はじめる」ボタンは `p-5` `text-lg` 以上(シニア配慮)
- [ ] スワイプは `react-native-reanimated` でフェード+スライドアップ

## 受入基準

- 初回起動時のみオンボーディングが表示される
- AsyncStorage クリア(設定画面の「はじめての方へ」再表示)で再度表示される
- 「エンタメ」表記がスライド内に明確にある(ストア審査要件 §12.5)

## 技術メモ

- 通知許可リクエストは行わない(REQUIREMENTS §5.3 の方針)
- スライド数は最大 4 まで。シニアが疲れる前に終わらせる
